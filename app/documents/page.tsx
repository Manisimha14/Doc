'use client';

import React from 'react';
import Dashboard from '../page';

/**
 * For now, the Documents view will leverage the Dashboard's robust grid 
 * but can be extended in the future for a more 'Finder' style interface.
 */
export default function DocumentsPage() {
  return <Dashboard />;
}
